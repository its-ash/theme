name: theme
description: "Flutter theme package: shared ThemeData, colors, typography, and a ThemeX component library."
version: 0.0.1
homepage: https://github.com/its-ash/theme
repository: https://github.com/its-ash/theme

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  file_picker: ^12.1.2
  flutter:
    sdk: flutter
  google_fonts: ^8.2.1
  markdown_widget: ^2.3.2+8
  video_player: ^2.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
